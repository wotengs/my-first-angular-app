import { Component } from '@angular/core';

@Component({
  selector: 'app-toast',
  standalone: true,
  template: `<!-- ngx-toastr used for native toasts; this stub remains for compatibility -->`,
})
export class ToastComponent {}
